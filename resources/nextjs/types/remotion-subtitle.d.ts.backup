declare module 'remotion-subtitle' {
    import { ComponentType } from 'react';

    export class SubtitleSequence {
        constructor(path: string);
        ready(): Promise<void>;
        getSequences(component: ComponentType<any>, fps: number): React.ReactNode[];
    }

    export const BounceCaption: ComponentType<{
        startFrame: number;
        endFrame: number;
        text: string;
        style?: React.CSSProperties;
    }>;
}